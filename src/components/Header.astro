---
---

<header class="site-header">
  <div class="container header-inner">
    <a href="/" class="brand" aria-label="PlayShip — home">PlayShip</a>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 0.5px solid var(--line);
    background: var(--bg);
  }
  .header-inner {
    display: flex;
    align-items: center;
    height: 60px;
  }
  .brand {
    font-family: var(--font-mono);
    font-weight: 500;
    font-size: 16px;
    letter-spacing: -0.01em;
    color: var(--ink);
    text-decoration: none;
  }
  .brand:hover {
    color: var(--approved);
  }
</style>
